/**
 * Example Firebase Configuration (do NOT commit your real keys).
 * Copy this file to `firebase-config.js` and replace the placeholders.
 */

const firebaseConfig = {
  apiKey: "AIzaSyREPLACE_WITH_YOURS",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID"
};

// Initialize Firebase (uncomment when values are filled)
// firebase.initializeApp(firebaseConfig);
// const db = firebase.firestore();
// const functions = firebase.functions();

console.log('Copy this file to frontend/firebase-config.js and fill values from Firebase Console');
